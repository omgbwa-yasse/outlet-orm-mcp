# Outlet ORM MCP Configuration
# Copy this file to .env and fill in your values

# === Required Configuration ===

# Path to your Outlet ORM project
# This is the root directory containing models/, controllers/, and database/ folders
OUTLET_ORM_ROOT=/path/to/your/outlet-orm-project

# === Database Configuration (Required for verification tools) ===
# These are optional for code generation tools but required for:
# - verify_model_schema
# - verify_relations
# - verify_migration_status
# - check_consistency

# Database driver: mysql, postgres, or sqlite
DB_DRIVER=mysql

# Database connection details
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=myapp
DB_USER=root
DB_PASSWORD=secret

# === SQLite Configuration Example ===
# Uncomment and modify if using SQLite
# DB_DRIVER=sqlite
# DB_DATABASE=/path/to/database.sqlite

# === PostgreSQL Configuration Example ===
# Uncomment and modify if using PostgreSQL
# DB_DRIVER=postgres
# DB_HOST=localhost
# DB_PORT=5432
# DB_DATABASE=myapp
# DB_USER=postgres
# DB_PASSWORD=secret

# === Optional Configuration ===

# Default output paths (relative to OUTLET_ORM_ROOT)
# Uncomment to customize
# OUTPUT_MODELS_PATH=models
# OUTPUT_CONTROLLERS_PATH=controllers
# OUTPUT_MIGRATIONS_PATH=database/migrations

# === Notes ===
#
# 1. Generation tools (generate_model, generate_controller, generate_migration)
#    only require OUTLET_ORM_ROOT
#
# 2. Verification tools require both OUTLET_ORM_ROOT and DB_* configuration
#
# 3. You can also provide dbConfig directly in tool calls instead of using
#    environment variables
#
# 4. Database drivers must be installed:
#    - MySQL: npm install mysql2
#    - PostgreSQL: npm install pg
#    - SQLite: npm install sqlite3
